# Environment for the qwen38-flash systemd unit. Same variables as scripts/serve.sh
# (see its header for the full list). Edit, then: sudo systemctl restart qwen38-flash
PORT=8000
# vLLM v0.30.0 base (Dockerfile.v0.30: docker build -f Dockerfile.v0.30 -t qwen38-flash-dgx:v0.30 .)
IMAGE=qwen38-flash-dgx:v0.30
HF_HUB_DIRS=/mnt/kc3000-nfs/cache/huggingface_hub:/home/willian/.cache/huggingface/hub
MODE=hybrid
YARN=1
CTX=500000
MTP=2
PREWARM=0
# compiled graphs are per base image
COMPILE_CACHE=qwen38v030
# journald can't render tqdm's \r-redrawn load bar (shows "[108B blob data]" instead)
EXTRA=--no-use-tqdm-on-load
